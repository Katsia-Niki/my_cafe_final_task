package by.jwd.cafe.model.entity;

/**
 * {@code UserRole} class
 * is enum that represents table 'users_role' in the database.
 */

public enum UserRole {
    UNSUPPORTED,
    CUSTOMER,
    ADMIN,
    GUEST
}
